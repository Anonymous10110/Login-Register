buildscript {
    repositories {
        google() // Required for Android dependencies
        mavenCentral() // Required for additional dependencies
    }

}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
